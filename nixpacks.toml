# nixpacks.toml
[phases.setup]
nixPkgs = ['nodejs_22']  # supabase-js pide engines.node >=22; alineado con el Dockerfile

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = ''  # vacío para static site